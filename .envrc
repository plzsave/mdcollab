# direnv 設定: このディレクトリに入ると .dev.vars を自動で環境変数化する。
# 初回だけ `direnv allow` が必要（以後はディレクトリへ cd するだけで読み込まれる）。
# .dev.vars は .gitignore 済み（秘密はそちら、この .envrc は読み込み指示だけなのでコミット可）。
# ファイルが無い環境（CI/本番）では何もしない＝実環境変数をそのまま使う。
dotenv_if_exists .dev.vars
